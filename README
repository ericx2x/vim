curl http://raw.github.com/yunong/dotvim/master/install.sh -L -k -o - | bash

Launch vim and type `:PluginInstall`

For fast finding in a project directory:
brew install fzf
brew install the_silver_searcher
